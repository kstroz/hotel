import { COLORS } from '@theme/Colors';
import { StyleSheet } from 'react-native';

export const verticalDividerStyles = StyleSheet.create({
  root: {
    height: '100%',
    width: 1,
    backgroundColor: COLORS.Gray,
  },
});
